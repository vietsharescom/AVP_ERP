{
    "version": "2.0.0",
    "tasks": [
        {"label": "Run All Tests", "type": "shell", "command": "npm test", "options": {"cwd": "${workspaceFolder}/webapp"},
         "group": {"kind": "test", "isDefault": true}},
        {"label": "Dev Server", "type": "shell", "command": "npm run dev", "options": {"cwd": "${workspaceFolder}/webapp"}}
    ]
}
