# FID-ERP-005_20260918.md
# Status Good/Hold (khâu Wrapping) — cổng QC thứ 2 trước Packing Slip
# Status: DONE (2026-09-18/19) — code xong, 55/55 test PASS ban đầu.
# v1.1 (2026-09-19): thêm PACK (đóng thùng) + Skid# — 5 test mới + 88 cũ
# không hồi quy = 93/93, lint sạch, build thành công. Xem Mục 11.
# v1.2 (2026-09-19): bắt buộc Traveler đã có SELECT trước khi cho ghi
# Kiểm tra Wrapping — 1 test mới + 16 cũ = 17/17. Xem Mục 12.
# v1.3 (2026-09-19): cảnh báo (không chặn) boxCount/qty phi lý so với
# part_control.qtyPerBox + shift không khớp quy ước MRNNG/AFTRN — 3 test
# mới + 17 cũ = 20/20. Xem Mục 13.
# v1.4 (2026-09-19): cảnh báo (không chặn) khi Traveler đã có 1 lần kiểm
# tra Wrapping trước đó (2 test mới = 22/22) + UI thêm bước "Tra" bắt
# buộc trước khi cho Xác nhận & Lưu. Xem Mục 14.
# v1.5 (2026-09-19): DONE — APPROVED ("ok code": Andy chốt câu 1, câu 2/3/4/6
# theo mặc định) rồi code xong, 201/201 test PASS, lint/tsc/build sạch, đã kiểm
# chứng bằng Chrome thật. Wrapping nhập nhanh giống FID-ERP-003 v1.4 + scan 2
# lần Traveler#/Part# bắt buộc. Xem Mục 15 (đề xuất) + Mục 16 (thực hiện).

---

## 1. INTENT
Sau khi Traveler qua máy lựa (FID-ERP-003), nhân viên kiểm tra thực tế
skid ở khâu Wrapping — đã dán label chưa? đã wrap chưa? đúng Traveler
chưa? — ghi 1 dòng **Good** hoặc **Hold** (kèm lý do nếu Hold, có thể
Concession đè lên Hold để vẫn cho xuất). Đồng thời ghi nhận thêm số
lượng lỗi phát hiện Ở KHÂU NÀY (`Reject`, khác lần lựa) nếu có.

**v1.1 (2026-09-19, khi viết FID-ERP-009)**: thêm **PACK** (đóng thùng —
số thùng + tổng qty + Skid#), ghi CÙNG lúc/CÙNG transaction với Good/Hold
— Andy xác nhận qua ảnh chụp `Data/4.WRAPPING/Wrapping_final.xlsm`
("WRAPPING SUMMARY"): cột `No. of boxes`/`TTL QNT`/`SKID#` nằm CÙNG 1
dòng với Good/Hold, tức là đóng thùng xảy ra tại khâu Wrapping (Xưởng),
KHÔNG phải việc Office làm sau ở FID-ERP-009 (đã hiểu nhầm ban đầu). Xem
Mục 11.

## 2. WHY
AVP_AI đã chạy thật pattern này 9 tháng (`FID-002_skid-status-good-hold.md`)
— chặn điều kiện thứ 2 (bên cạnh QC lúc lựa) trước khi cho tạo Packing
Slip, thay việc đánh dấu tay trên giấy in (mất dấu vết điện tử, gap đã
ghi từ THIET_KE_HE_THONG_MOI.md Phần 13.2).

Andy chỉ ra dữ liệu thật `Data/4.WRAPPING/Wrapping_final.xlsm` (CHECKING
SUMMARY) có cột `Reject` — kiểm chứng (2143 dòng): là SỐ LƯỢNG lỗi phát
hiện THÊM ở Wrapping (khớp theo thứ tự với tên lỗi trong `Special
Notes`, vd `Reject="3, 100, 1, 7"` ↔ `Special Notes="MIXED, SILVERS,
DAMAGED LOCKING, DAMAGED FLANGE"`) — KHÔNG phải 1 giá trị trạng thái thứ
4. Đối chiếu Odoo: `stock.scrap` (số lượng+lý do) và `quality.check`
(Pass/Fail) là 2 model tách riêng — không có mẫu gộp số lượng vào 1 enum
trạng thái. Andy xác nhận hướng: `Reject` → ghi thêm `SCRAP` (dùng lại
`move_type` đã có); Good/Hold/Concession → bảng MỚI `quality_checks`
(FID-ERP-001 v1.6, xem file đó Mục 12).

## 3. MODULE
```
Primary:  webapp/app/api/quality/check/route.ts (POST — ghi 0..n SCRAP
          (reject) + 0..1 PACK (v1.1, đóng thùng) + 1 quality_checks
          trong 1 transaction chính, riêng travelers.skidNo cập nhật ở
          lệnh kế tiếp — xem Mục 5)
          webapp/app/wrapping/check/page.tsx (UI kiểm tra Wrapping, v1.1
          thêm ô PACK)
Affected: webapp/prisma/schema.prisma (FID-ERP-001 lên v1.10 — thêm cột
          `boxCount` trên `stock_moves` + `skidNo` trên `travelers`, v1.1)
NOT:      Không tự động chặn tạo Packing Slip dựa trên quality_checks —
          đó là FID-ERP-009 (đọc `traveler_last_quality_check` lúc build
          điều kiện dòng đủ điều kiện xuất), CHƯA làm ở đây. Không mở
          rộng `defect_types` với tên lỗi mới quan sát được ở Wrapping
          (`DAMAGED FLANGE`, `DAMAGED LOCKING` — không khớp 10 giá trị
          hiện có) — để dành, cần Andy xác nhận riêng (xem Mục 8). PACK
          (v1.1) KHÔNG chia theo từng thùng riêng lẻ — chỉ 1 dòng PACK
          tổng hợp (số thùng + tổng qty) mỗi lần ghi, đúng độ chi tiết dữ
          liệu thật (1 dòng CHECKING SUMMARY/lần kiểm tra).
```

## 4. CONTRACT

### `POST /api/quality/check`
Input:
```json
{
  "travelerNo": "718779",
  "status": "GOOD",
  "checkedBy": "391",
  "pack": { "boxCount": 32, "qty": 4800, "skidNo": "77", "machineCode": "TBL", "shift": "AFTRN" }
}
```
`reject` optional — không có thì bỏ qua, không ghi `SCRAP` nào.
`concession` optional — CHỈ hợp lệ khi `status="HOLD"` (nhượng bộ là đè
lên Hold, không có ý nghĩa khi Good).
**`pack` optional (MỚI v1.1)** — khi có, TẤT CẢ sub-field bắt buộc
(`boxCount`, `qty`, `skidNo`, `machineCode`, `shift`) — không có trường
hợp chỉ điền 1 phần (đúng dữ liệu thật, luôn ghi đủ cùng 1 dòng).

Output:
```json
{ "ok": true, "qualityCheckId": 50, "scrapMoveIds": [], "totalRejectQty": 0, "packMoveId": 51, "warnings": [] }
```
`packMoveId` chỉ có khi request có `pack`.

**(v1.3) `warnings`** — KHÔNG chặn, chỉ cảnh báo, áp dụng khi có `pack`:
- `pack.shift` không khớp quy ước `MRNNG`/`AFTRN` (`lib/shift.ts`).
- `pack.qty`/`pack.boxCount` phi lý so với `part_control.qtyPerBox` của
  đúng Part# Traveler đó — hoặc vượt sức chứa (`qty > boxCount ×
  qtyPerBox`), hoặc trung bình pcs/thùng quá thấp (< 50% `qtyPerBox`).

**(v1.4) `warnings`** — thêm 1 cảnh báo LUÔN kiểm tra (không cần có
`pack`): Traveler đã có ÍT NHẤT 1 lần `quality_checks` trước đó (bất kỳ
status nào) → cảnh báo rõ status/thời điểm lần trước, KHÔNG chặn ghi
thêm (giống pattern `skippedDuplicates` ở FID-ERP-002 — ca thật: kiểm
tra lại sau khi sửa lỗi là hợp lệ).

## 5. RULES
MUST:
  - `travelerNo` PHẢI đã tồn tại trong `travelers` — 400 rõ ràng nếu
    không, không phải lỗi FK thô (cùng nguyên tắc FID-ERP-003).
  - **(v1.2) `travelerNo` PHẢI đã có ÍT NHẤT 1 dòng `stock_moves` loại
    `SELECT`** — Kiểm tra Wrapping là kiểm tra hàng ĐÃ QUA MÁY LỰA, chưa
    SELECT thì chưa có gì để kiểm tra — 400 rõ ràng, không ghi gì. Andy
    phát hiện qua test thật 2026-09-19: route trước đó không chặn, lưu
    được Good cho Traveler chưa hề sản xuất.
  - `status` chỉ `"GOOD"` hoặc `"HOLD"`.
  - `status="HOLD"` → `note` bắt buộc non-empty (lý do Hold).
  - `checkedBy` bắt buộc non-empty — người kiểm tra (CCP-1, xác nhận
    trách nhiệm giống mọi FID khác đã có).
  - Mỗi dòng `reject[]`: `qty` > 0, `reasonCode` phải tồn tại trong
    `defect_types` (validate sớm ở API, giống FID-ERP-003) — ghi thành
    `SCRAP` với `operatorCode = checkedBy` (phục vụ audit "ai phát hiện
    lỗi này"), `sourceStation="FACTORY"` (Wrapping vẫn ở Xưởng, Máy 3).
  - `concession` chỉ hợp lệ khi `status="HOLD"` — có `concession` mà
    `status="GOOD"` → 400 (xem MUST NOT).
  - Có `concession` → `concession.by` và `concession.reason` đều bắt
    buộc non-empty.
  - Ghi trong 1 transaction: 0..n `SCRAP` (reject) + 1 `quality_checks`
    — ghi đủ hoặc không ghi gì.
  - `totalRejectQty` = tổng `qty` của `reject[]` đã ghi — TỰ TÍNH, không
    phải ô gõ tay riêng (cùng nguyên tắc `totalDefectQty` ở FID-ERP-003).
  - **(v1.1) `pack.qty` KHÔNG được vượt quá số đã lựa còn có thể đóng gói**
    — tính `available = SUM(stock_moves.qty WHERE moveType='SELECT') -
    SUM(stock_moves.qty WHERE moveType='PACK')` của đúng `travelerNo` đó
    (trước request này); `pack.qty > available` → 400 rõ ràng, không ghi
    gì (tránh đóng gói nhiều hơn số thực có, kể cả cộng dồn nhiều lần
    PACK cho cùng Traveler).
  - **(v1.1) `pack.machineCode`/`pack.shift` chuẩn hoá TRIM+UPPER** trước
    khi ghi (đúng pattern FID-ERP-003) — bắt buộc vì CHECK constraint
    `chk_select_pack_requires_context` (FID-ERP-001) đòi `machine_code`/
    `operator_code`/`shift` non-empty cho `move_type='PACK'`.
    `operatorCode` của dòng PACK = `checkedBy` (cùng người, giống cách
    `reject`/SCRAP dùng `checkedBy`).
  - **(v1.1) `travelers.skidNo` cập nhật Ở LỆNH RIÊNG** (không cùng mảng
    `$transaction` với SCRAP/PACK/quality_checks) — vì `Traveler` là
    Model KHÁC `StockMove`/`QualityCheck` (không có cột `id`), trộn
    chung mảng sẽ làm TypeScript suy ra kiểu UNION mất `.id` trên các
    phần tử khác — đúng bài học đã gặp ở FID-ERP-006 §10. Đúng đắn
    nghiệp vụ không đổi: PACK/quality_checks chắc chắn ghi xong trước
    khi cập nhật `skidNo`.
  - **(v1.3) `pack.shift` không khớp `MRNNG`/`AFTRN`, hoặc `pack.qty`/
    `pack.boxCount` phi lý so với `part_control.qtyPerBox`** → thêm vào
    `warnings`, KHÔNG chặn ghi. Andy phát hiện qua test thật: gõ
    `boxCount=700` cho `qty=1000` (Part# có `qtyPerBox=6000`, tức trung
    bình 1.4 pcs/thùng) vẫn được lưu — nhiều khả năng gõ lộn 2 ô, nhưng
    KHÔNG chặn cứng vì đây là số Xưởng tự nhập tay (không có draft OCR
    để sửa trước), thùng cuối đóng lẻ cũng là ca hợp lệ có thật.
  - **(v1.4) Traveler đã có 1 lần `quality_checks` trước đó** → thêm vào
    `warnings` (status + thời điểm lần trước), KHÔNG chặn ghi thêm —
    cùng nguyên tắc "cảnh báo, không âm thầm bỏ qua/ghi đè" đã dùng cho
    RECEIVE trùng ở FID-ERP-002. Andy phát hiện qua test thật: bấm lại
    nhiều lần cùng Traveler tạo nhiều dòng `quality_checks` trùng, im
    lặng không cảnh báo.
  - **(v1.4, UI) `/wrapping/check` thêm bước "Tra" BẮT BUỘC trước khi
    cho phép Xác nhận & Lưu** — gõ Traveler#, bấm "Tra" (gọi lại
    `/api/search` sẵn có ở FID-ERP-004, không viết route riêng) để xem
    Part#/PO#/Lot#/trạng thái gần nhất TRƯỚC khi sửa/lưu; đổi Traveler#
    sau khi đã tra thì lookup cũ mất hiệu lực, phải tra lại. Andy yêu
    cầu sau khi phát hiện gõ xong bấm Lưu ngay không có gì xem lại
    trước — đây là cải tiến UI, KHÔNG đổi hợp đồng API `confirm`.
MUST NOT:
  - Không cho `concession` khi `status="GOOD"` — 400 ngay, không âm
    thầm bỏ qua field lạ.
  - Không UPDATE dòng `quality_checks` cũ khi có Concession sau này —
    ghi 1 dòng MỚI (status=HOLD + concession fields), đúng append-only
    (khác AVP_AI, nơi Concession UPDATE thẳng lên dòng cũ — xem
    FID-ERP-001 v1.6 Mục 12).
  - Không tự thêm giá trị mới vào `defect_types` dù dữ liệu thật Wrapping
    có tên lỗi không khớp (xem Mục 8) — dùng đúng 10 giá trị hiện có,
    nếu không khớp thì để trống `reasonCode`/không ghi reject dòng đó,
    KHÔNG tự bịa mã mới.

## 6. EXAMPLE
Traveler `718039` kiểm tra ở Wrapping — thiếu label (Hold), phát hiện
thêm 5 pcs lỗi `MIXED`, sau đó khách đồng ý nhận (Concession):
```
POST /api/quality/check
{ "travelerNo": "718039", "status": "HOLD", "note": "Chưa dán label",
  "checkedBy": "290", "reject": [{ "reasonCode": "MIXED", "qty": 5 }],
  "concession": { "by": "111", "reason": "Khách đồng ý nhận dù thiếu label" } }
→ stock_moves: +1 SCRAP qty=5 reason_code=MIXED operator_code=290 source_station=FACTORY
→ quality_checks: +1 dòng status=HOLD note="Chưa dán label" checked_by=290
                     concession_by=111 concession_reason="..." concession_at=now()
```
Kiểm tra khác, không có gì bất thường: `{ "travelerNo": "718960", "status": "GOOD", "checkedBy": "290" }`
→ chỉ ghi 1 dòng `quality_checks` status=GOOD, không có `SCRAP`.

**v1.1** — Traveler `718779` (Part# `40073474`) đã lựa 4800 pcs, kiểm tra
Wrapping Good, đóng 32 thùng, Skid# 77 (khớp ảnh chụp thật Andy gửi):
```
POST /api/quality/check
{ "travelerNo": "718779", "status": "GOOD", "checkedBy": "391",
  "pack": { "boxCount": 32, "qty": 4800, "skidNo": "77", "machineCode": "TBL", "shift": "AFTRN" } }
→ stock_moves: +1 PACK qty=4800 box_count=32 machine_code="TBL" shift="AFTRN" operator_code="391"
→ quality_checks: +1 dòng status=GOOD
→ travelers.skidNo = "77"
```
Edge: Traveler chỉ lựa được 1000 pcs, gửi `pack.qty=1500` → 400 (vượt số
đã lựa), không ghi gì.

## 7. TEST CRITERIA
- [ ] `travelerNo` không tồn tại → 400, không ghi gì
- [ ] **(v1.2)** `travelerNo` tồn tại nhưng CHƯA có `SELECT` nào → 400, không ghi `quality_checks`
- [ ] `status="HOLD"` thiếu `note` → 400 trước khi chạm DB
- [ ] `concession` gửi kèm `status="GOOD"` → 400
- [ ] Có `concession` nhưng thiếu `by`/`reason` → 400
- [ ] `reject[].qty <= 0` hoặc `reasonCode` không tồn tại → 400 trước khi chạm DB
- [ ] `status="GOOD"`, không `reject` → ghi đúng 1 `quality_checks` (concession fields null), không có `SCRAP` nào
- [ ] `status="HOLD"` + 1 `reject` → ghi 1 `quality_checks` (HOLD) + 1 `SCRAP` trong 1 transaction, `operator_code`=`checkedBy`
- [ ] `status="HOLD"` + `concession` hợp lệ → `quality_checks` có đủ `concessionBy`/`concessionReason`/`concessionAt`
- [ ] `totalRejectQty` trong response = đúng tổng `qty` của `reject[]`
- [ ] `traveler_last_quality_check` VIEW trả đúng dòng mới nhất khi 1 Traveler có nhiều lần check
- [ ] `UPDATE`/`DELETE` trực tiếp `quality_checks` → trigger chặn, raise exception (append-only ở tầng DB)
- [ ] Tất cả test FID-ERP-001..004 vẫn PASS (no regression)
- [ ] CHANGELOG.md cập nhật
- [ ] **(v1.1)** `pack` thiếu 1 trong `boxCount`/`qty`/`skidNo`/`machineCode`/`shift` → 400
- [ ] **(v1.1)** `pack.qty` vượt số đã lựa còn có thể đóng gói → 400, không ghi `PACK` nào
- [ ] **(v1.1)** `pack` hợp lệ → ghi đúng 1 `PACK` (kèm `boxCount`) + cập nhật `travelers.skidNo`, cùng lúc với `quality_checks`
- [ ] **(v1.1)** Gọi `pack` 2 lần cộng dồn không vượt SELECT, lần 2 vượt phần còn lại → 400
- [ ] **(v1.1)** Không gửi `pack` → hành vi y hệt trước v1.1 (no regression), không có `PACK` nào
- [ ] **(v1.3)** `pack.qty` vượt sức chứa `boxCount × qtyPerBox` → vẫn 200, `warnings` báo rõ
- [ ] **(v1.3)** Trung bình pcs/thùng < 50% `qtyPerBox` → vẫn 200, `warnings` báo "thấp bất thường"
- [ ] **(v1.3)** `pack.shift` không khớp `MRNNG`/`AFTRN` → vẫn 200, `warnings` báo rõ
- [ ] **(v1.4)** Lần kiểm tra ĐẦU TIÊN của Traveler → `warnings` KHÔNG có cảnh báo trùng lặp
- [ ] **(v1.4)** Lần kiểm tra THỨ 2 trở đi cùng Traveler → vẫn 200, `warnings` báo đã có lần trước (status/thời điểm), ghi thêm dòng `quality_checks` mới bình thường

## 8. NOT IN SCOPE
- **Chặn tạo Packing Slip dựa trên `quality_checks`** — thuộc FID-ERP-009
  (đọc `traveler_last_quality_check`, áp lại điều kiện dòng đủ điều kiện
  xuất — tương đương `packingListLine.ts` AVP_AI đã có).
- **Mở rộng `defect_types`** với tên lỗi thật quan sát ở Wrapping không
  khớp 10 giá trị hiện có (`DAMAGED FLANGE`, `DAMAGED LOCKING`, ...) —
  CHƯA quyết, cần Andy xác nhận riêng trước khi thêm (giống quy trình đã
  làm với `REWORK`, FID-ERP-001 v1.5). Tạm thời reject không khớp mã nào
  thì KHÔNG ghi được dòng đó qua route này (400) — người dùng phải chọn
  đúng 1 trong 10 mã, hoặc bỏ qua reject đó.
- **Disambiguation nhiều dòng/lần lựa cùng 1 Traveler** (AVP_AI cần vì 1
  Traveler có thể có nhiều `lotNo` khác nhau qua nhiều lần WorkStationArchive)
  — KHÔNG áp dụng ở AVP_ERP vì `Traveler.lotNo` đã CHỐT 1:1 (FID-ERP-001
  v1.3) — không có tình huống cần chọn "đúng dòng nào" như AVP_AI.
- **UI hiển thị lịch sử toàn bộ `quality_checks` của 1 Traveler** (không
  chỉ dòng gần nhất) — chưa cần ở FID này, `traveler_last_quality_check`
  đủ cho mục đích chặn Packing Slip.

## 9. FILES
New:    webapp/app/api/quality/check/route.ts
        webapp/app/wrapping/check/page.tsx
        webapp/tests/integration/quality-check.test.ts
Update: CHANGELOG.md (bắt buộc)
        webapp/prisma/schema.prisma (v1.10 — cột `boxCount`/`skidNo`, v1.1)
        webapp/prisma/migrations/ (migration mới, v1.1)

## 10. THỰC HIỆN (2026-09-18, sau khi APPROVED) — ghi chú lệch so với kế hoạch

- **Cast `status as "GOOD" | "HOLD"`**: TypeScript không tự suy ra
  `status: string` (đã validate qua `Set`) khớp union `QualityStatus` của
  Prisma — cast tường minh sau khi validate, không đổi logic.
- Không cần route `GET` lookup (khác AVP_AI) — đúng dự tính ở Mục 8, vì
  `Traveler.lotNo` đã 1:1, không có disambiguation nào cần chọn.
- Test append-only (`UPDATE`/`DELETE` bị chặn) viết theo đúng pattern
  `fid-erp-001.test.ts` — xác nhận trigger riêng
  `prevent_quality_checks_mutation` hoạt động đúng, không lẫn với trigger
  `stock_moves`.

**Kết quả**: `npm test` → 55/55 PASS (11 mới + 44 cũ) · `npm run lint` →
sạch · `npm run build` → thành công. Xem CHANGELOG.md.

## 11. THỰC HIỆN v1.1 (2026-09-19, khi viết FID-ERP-009) — thêm PACK + Skid#

Phát hiện khi bắt đầu thiết kế FID-ERP-009 (Packing Slip): hỏi Andy "PACK
là gì, có phải Wrapping không" → Andy gửi ảnh chụp thật `WRAPPING
SUMMARY - INFASCO` (`Data/4.WRAPPING/Wrapping_final.xlsm`) — xác nhận cột
`No. of boxes`/`TTL QNT`/`SKID#` nằm CÙNG 1 dòng với Good/Hold, tức đóng
thùng (PACK) xảy ra ngay tại khâu Wrapping — Andy xác nhận sửa lại
FID-ERP-005 (dù đã DONE) thay vì để Office tự nhập lại ở FID-ERP-009.

Thêm vào request `POST /api/quality/check`: `pack` optional (`boxCount`,
`qty`, `skidNo`, `machineCode`, `shift`) — khi có, ghi thêm 1 dòng
`stock_moves` `moveType='PACK'` (cùng transaction với SCRAP/quality_checks)
+ cập nhật `travelers.skidNo` (lệnh riêng ngay sau, xem RULES). Schema
v1.10 (FID-ERP-001): thêm cột `boxCount` (Int?) trên `stock_moves` + cột
`skidNo` (String?, mutable) trên `travelers`. Migration
`webapp/prisma/migrations/20260919031538_add_pack_boxcount_and_skidno`.

Validate `pack.qty` không vượt `SUM(SELECT) - SUM(PACK)` hiện có của
Traveler đó — tránh đóng gói vượt số thực đã lựa, kể cả cộng dồn nhiều
lần PACK. Không thêm bảng/model mới, không đổi `MoveType` enum (PACK đã
có sẵn từ v1.2, chỉ là route ĐẦU TIÊN dùng tới, giống RETURN ở FID-ERP-007).

**Kết quả**: `npm test` → 93/93 PASS (5 mới + 88 cũ) · `npm run lint` →
sạch · `npm run build` → thành công. Xem CHANGELOG.md.

## 12. THỰC HIỆN v1.2 (2026-09-19, cùng phiên) — bắt buộc SELECT trước khi Wrapping

Phát hiện qua test thật trên trình duyệt: Andy nhập Traveler `717844`
(chưa hề có `SELECT` nào — chỉ mới đăng ký `poNo`, chưa qua Xưởng) vào
`/wrapping/check`, bấm "Good" rồi "Xác nhận & Lưu" — hệ thống **vẫn cho
lưu thành công** (2 dòng `quality_checks` thật đã ghi, không xoá được vì
`quality_checks` append-only — chấp nhận còn lại vĩnh viễn trong DB dev,
vô hại). Route trước đó chỉ validate Traveler TỒN TẠI, không validate đã
qua SELECT — sai theo đúng logic nghiệp vụ Mục 1 ("sau khi Traveler qua
máy lựa"). Andy xác nhận cần chặn.

Thêm 1 lệnh `stockMove.findFirst({ moveType: "SELECT" })` NGAY SAU khi
xác nhận Traveler tồn tại, TRƯỚC bước validate `pack` (để chặn sớm nhất
có thể, tránh tính toán `available` vô nghĩa nếu chưa từng SELECT). Sửa
fixture test (`tests/integration/quality-check.test.ts`) — `makeTraveler()`
giờ tự động kèm 1 dòng SELECT (mọi test dùng chung), tách riêng
`makeTravelerBare()` cho nhóm test "pack" (vốn tự tạo SELECT với qty
chính xác riêng qua `selectFor()`, cần tránh cộng dồn 2 lần SELECT nếu
dùng chung `makeTraveler()`). 1 test mới ("Traveler chưa từng có SELECT
→ 400") + 16 test cũ sửa fixture không hồi quy = **17/17 PASS**, lint
sạch, `tsc --noEmit` sạch, build thành công.

## 13. THỰC HIỆN v1.3 (2026-09-19, cùng phiên) — cảnh báo boxCount/qty + shift

Ngay sau khi sửa v1.2, Andy nhận ra dữ liệu test trước đó cũng có
`boxCount=700`/`qty=1000` (Part# `qtyPerBox=6000` trong `part_control`)
— trung bình 1.4 pcs/thùng, rõ ràng phi lý (nhiều khả năng gõ lộn 2 ô)
nhưng route vẫn cho lưu không cảnh báo gì. Đồng thời Andy chỉ ra
`pack.shift="MONING"` (gõ nhầm) cũng không bị phát hiện.

Thêm 2 nhóm cảnh báo (KHÔNG chặn — số Xưởng tự nhập tay, không có
draft/OCR để sửa trước, chặn cứng có thể cản ca thật hợp lệ):
1. Đối chiếu `pack.qty`/`pack.boxCount` với `part_control.qtyPerBox`
   (tra theo `traveler.partNo`) — báo nếu vượt sức chứa
   (`qty > boxCount × qtyPerBox`) hoặc trung bình pcs/thùng quá thấp
   (`< 50% qtyPerBox`).
2. Đối chiếu `pack.shift` với quy ước `MRNNG`/`AFTRN` (`lib/shift.ts`,
   viết chung với FID-ERP-003 — xem file đó Mục 11 cho nguồn chứng từ
   thật `WRAPPING SUMMARY` xác nhận quy ước này).

Route trả thêm field `warnings: string[]` (rỗng nếu không có gì bất
thường) — UI `/wrapping/check` hiện dạng cảnh báo màu cam, giống pattern
đã dùng ở `/capture` (FID-ERP-002). 3 test mới (vượt sức chứa; trung
bình quá thấp; shift lệch quy ước) + 17 test cũ không hồi quy = **20/20
PASS**, 176/176 toàn dự án lúc đó, lint sạch, `tsc --noEmit` sạch, build
thành công.

## 14. THỰC HIỆN v1.4 (2026-09-19, cùng phiên) — cảnh báo trùng lặp + bước "Tra"

Andy tự test tiếp: bấm "Xác nhận & Lưu" nhiều lần liên tiếp cho cùng
Traveler `716958` (gõ lại số, giữ nguyên "Người kiểm tra" còn sót từ lần
trước) — mỗi lần đều ghi thêm 1 dòng `quality_checks` GOOD mới, không
cảnh báo gì (4 dòng trùng trong DB dev). Andy hỏi lý do, rồi yêu cầu
thiết kế lại để có bước "scan (tra) và verify lại trước" cho cả Traveler
(FID-ERP-003) và Wrapping (FID-ERP-005).

**Phần 1 — cảnh báo trùng lặp (API)**: thêm 1 lệnh
`prisma.qualityCheck.findFirst({ travelerNo, orderBy: { createdAt: "desc" } })`
TRƯỚC transaction ghi — nếu có, đẩy 1 dòng vào `warnings` (không chặn,
đúng pattern `skippedDuplicates` FID-ERP-002).

**Phần 2 — bước "Tra" bắt buộc (UI)**: `/wrapping/check` VÀ
`/factory/select` (FID-ERP-003 v1.3) đều thêm nút "Tra" cạnh ô
Traveler# — gọi lại `GET /api/search?q=<travelerNo>` sẵn có
(FID-ERP-004), tìm đúng bản ghi khớp `travelerNo` trong `data.travelers`,
hiện Part#/PO#/Lot#/`lastMoveType`/đã xuất chưa. `canSubmit` giờ đòi
`lookup.travelerNo === travelerNo` (so khớp CHÍNH XÁC, không phải chỉ
"đã tra 1 lần nào đó") — đổi nội dung ô Traveler# sau khi tra thì
`lookup` bị xoá (`onTravelerNoChange`), bắt tra lại, tránh xác nhận nhầm
thông tin Traveler khác. Không viết route lookup riêng — tái dùng
`/api/search` đã có sẵn đủ dữ liệu cần thiết, đúng nguyên tắc không xây
trùng lặp.

Không có test tự động cho phần UI (project chưa có hạ tầng test
component, giống lý do đã ghi ở FID-ERP-014 Mục 10) — xác nhận qua
`tsc --noEmit` + `next build` + Andy tự test lại trên trình duyệt thật.
2 test mới cho cảnh báo trùng lặp (lần đầu không cảnh báo; lần 2 có cảnh
báo, vẫn ghi thêm) + 20 test cũ không hồi quy = **22/22 PASS**
(`webapp/tests/integration/quality-check.test.ts`), 178/178 toàn dự án,
lint sạch, `tsc --noEmit` sạch, build thành công.

## 15. v1.5 (2026-09-19) — ĐỀ XUẤT: Wrapping nhập nhanh, scan 2 lần (Traveler# + Part#) (CHỜ ANDY DUYỆT, CHƯA CODE)

### 15.0 Bối cảnh
Andy: "làm tương tự cho wrapping" sau khi FID-ERP-003 v1.4 xong (scan Traveler#
tự điền từ DB + tự tính + bố cục giống giấy, không OCR). Andy thêm: **trên biểu
mẫu Part# và Traveler# dùng CHÉO NHAU để tránh lộn → thiết kế mới cũng scanner
2 lần** (scan Traveler#, rồi scan Part# — 2 mã phải khớp nhau).

Dữ liệu thật (`Data/4.WRAPPING/Wrapping_final.xlsm`, CHECKING SUMMARY, 2143
dòng — kiểm chứng `openpyxl` 2026-09-19):
- **100% dòng** có số thùng > 0; 2142/2143 có Skid# → đóng thùng (PACK) xảy ra ở
  gần như MỌI lần Wrapping.
- **2023 dòng (94%)** có `TTL QNT = No. of boxes × qty_per_box` (theo
  `part_control`); 43 dòng thấp hơn, 43 dòng cao hơn (thùng cuối thiếu/thừa), 34
  dòng không có Part# trong `part_control` → tự tính Total đúng với đại đa số.
- **836 dòng (39%)** có Reject + Special Notes — nhiều, không hiếm. Tên lỗi hay
  gặp: `MIXED` 652, `STUCK TGT` 246, `EXCESS PLATING` 74, `SILVERS` 38 → đều có
  sẵn trong `defect_types`. Tên lỗi KHÔNG có trong `defect_types`: `DAMAGED` 30,
  `MISSING TAB` 9, `DAMAGED FLANGE` 8, `CRACKED` 7, `MISSING COLOR` 7,
  `DAMAGED LOCKING` 5 (≈3% dòng).
- Cột `Serial`/`SERIAL2` trong CHECKING SUMMARY **rỗng 100%** — dữ liệu Serial
  nằm ở `WorkStationArchive` (FINISHED PALLET REPORT), không phải ở đây → Serial
  tách FID riêng (xem 15.6).

### 15.1 INTENT
`/wrapping/check`: scan Traveler# + Enter → form TỰ ĐIỀN từ DB (Part#, Lot, Pot,
PO, Pcs/Carton, số đã lựa/đã đóng/còn có thể đóng); scan tiếp Part# để đối chiếu
(**bắt buộc**); QA chỉ nhập số thùng (TTL tự tính), Skid#, máy, người kiểm tra,
ca, Good/Hold và reject nếu có. Contract `POST /api/quality/check` KHÔNG đổi.

### 15.2 Scan 2 lần — quy tắc chéo Traveler#/Part# (Andy, 2026-09-19)
1. Scan Traveler# + Enter → tự Tra → con trỏ nhảy sang ô "Part# (scan đối chiếu)".
2. Scan Part# (Part# in trên tờ, có thể kèm hậu tố — so sau `stripPartSuffix`,
   không phân biệt hoa/thường; scan mã "Finished Part Number" cũng khớp vì cùng
   mã gốc) + Enter → con trỏ nhảy sang ô số thùng.
3. **Part# scan phải KHỚP Part# của Traveler trong DB mới cho lưu** — chưa scan
   hoặc lệch → nút "Xác nhận & Lưu" bị khoá + báo đỏ "Part# scan KHÁC Part# của
   Traveler này — có thể nhầm tờ". (Khác FID-ERP-003 v1.4 hiện đang chỉ CẢNH BÁO —
   xem 15.9 để đồng bộ.) Đổi Traveler# thì Part# scan bị xoá, scan lại.

### 15.3 Nguồn từng trường (theo thứ tự cột CHECKING SUMMARY)
| Trường (nhãn giấy/sheet) | Nguồn mới |
|---|---|
| TRAVELER | Scan #1 (hoặc gõ) + Enter → Tra |
| PART# | Xám, tự điền từ DB; + ô scan #2 đối chiếu (bắt buộc, vàng) |
| POT#, LOT#, PO | Xám, tự điền từ DB (chỉ để đối chiếu, form này không ghi) |
| Pcs/Carton | Xám, từ `part_control.qty_per_box` |
| Đã lựa / Đã đóng / Còn có thể đóng | Xám: `SUM(SELECT)`, `SUM(PACK)`, hiệu 2 số (đúng công thức server đang chặn `pack.qty` vượt quá) |
| SHIFT | 2 nút `MRNNG`/`AFTRN`, không điền sẵn |
| Machine / MC# | Vàng; nút gợi ý "Theo lần lựa: MC112" (máy của dòng SELECT gần nhất của Traveler này) — bấm để điền |
| CHECKER (người kiểm tra) | Vàng; nút "Lần trước: 391" (`localStorage`) — bấm để điền, KHÔNG điền sẵn |
| No. of boxes | Vàng; nút gợi ý "Đóng hết: 32 thùng · 4800" (chỉ hiện khi `còn` chia hết `qty_per_box`) — bấm để điền số thùng |
| TTL QNT | Tự tính = thùng × Pcs/Carton, sửa tay được (thùng cuối thiếu) |
| SKID# | Vàng, gõ tay |
| Good / Hold | 2 nút (mặc định Good như hiện nay); Hold → bắt buộc lý do |
| Reject | Bảng liệt kê sẵn đủ các loại defect theo thứ tự tờ giấy Traveler (giống `/factory/select`), gõ số ở dòng nào có; thay cho ô gõ tay mã `LOOSE_WASHER_NUT` ở v1.4 |
| Special Notes / Notes | Ô ghi chú luôn có (không chỉ khi Hold), tuỳ chọn |
| Concession | Như cũ (chỉ khi Hold) |

**PACK mặc định BẬT** khi `còn có thể đóng > 0` (100% dòng dữ liệu thật có đóng
thùng); mặc định TẮT + khoá khi `còn = 0` ("đã đóng gói hết") — tránh PACK trùng.

### 15.4 CONTRACT
**Route mới** `GET /api/quality/lookup?travelerNo=718779` (thuộc `/api/quality/*`
— chỉ trạm Xưởng, `lib/auth.ts` sẵn có; CHỈ đọc):
```json
{ "ok": true, "traveler": { "travelerNo": "718779", "partNo": "40073474",
  "poNo": "194081", "potNo": "1768", "lotNo": "6-259-07-C", "qtyPerBox": 150,
  "selectedQty": 4800, "packedQty": 0, "availableToPack": 4800,
  "lastSelect": { "machineCode": "TBL2", "operatorCode": "391", "shift": "AFTRN" },
  "lastQualityCheck": { "status": "GOOD", "createdAt": "..." },
  "shipped": false, "lastMoveType": "SELECT" } }
```
`lastSelect`/`lastQualityCheck` = `null` nếu chưa có. Không có Traveler → 404.
`POST /api/quality/check` — request/response KHÔNG đổi; các cảnh báo v1.3/v1.4 vẫn
nguyên. Lấy defect list cho bảng Reject từ `GET /api/factory/defect-types`
(sẵn có, cùng trạm Xưởng).

### 15.5 RULES
MUST:
  - Scan 2 lần bắt buộc (15.2): `canSubmit` đòi Part# scan khớp DB.
  - TTL tự tính; sửa tay được; UI chặn TTL > `availableToPack` (server vẫn chặn — 400
    như v1.1, UI báo trước cho khỏi mất 1 vòng gửi).
  - Máy/người kiểm tra/ca ô TRỐNG mỗi Traveler mới, chỉ có nút gợi ý (lý do như
    FID-ERP-003 §13.8 câu 2: tránh quên đổi → ghi sai vào sổ cái bất biến).
  - Đọc `lookup` chỉ để hiển thị/gợi ý — server `POST` vẫn validate lại toàn bộ
    (không tin số liệu client).
MUST NOT:
  - Không AI/OCR. Không ghi Pot/Lot/Part# từ form này. Không đổi contract `POST`.
  - Không tự đoán Ca theo giờ. Không điền sẵn máy/người.

### 15.6 NOT IN SCOPE
- **Serial từng thùng** (`carton_serials`, scan từng tem) — FID riêng. Chờ Andy
  trả lời 2 câu chưa có đáp án: tem có 1 barcode gộp hay 3 barcode riêng
  (Traveler/Part/Serial)? máy scan là loại nào (USB, tự Enter)? Không thiết kế mù.
- Mở rộng `defect_types` cho `DAMAGED`/`MISSING TAB`/`DAMAGED FLANGE`/`CRACKED`/
  `MISSING COLOR`/`DAMAGED LOCKING` (việc mở #6) — tạm dùng dòng `Others` + Special
  Notes (xem câu hỏi 4 ở 15.8).
- Các cột CHECKING SUMMARY chưa có trong schema ERP: `Type` (Nut/Blt-Scrw), `O/C`,
  `S/P` (`S/P` 1586 dòng, `Crt.&P` 523 dòng, `Cert.&P` 34 dòng), `Off`, `LOCATION`
  (luôn `F` — đã quyết không có bảng `locations`, FID-ERP-001) — không thêm ở FID này.
- In sticker — FID-ERP-010.

### 15.7 TEST CRITERIA
- [ ] `lookup`: đúng `selectedQty`/`packedQty`/`availableToPack` sau khi có SELECT và sau PACK 1 phần; `lastSelect` đúng dòng SELECT MỚI NHẤT; 404 khi không có Traveler
- [ ] `lookup`: `qtyPerBox` đúng Part# của Traveler (không lẫn Part# chứa cùng chuỗi)
- [ ] Hàm thuần dùng chung `lib/factoryEntry.ts`: `suggestPackAll(available, qtyPerBox)` → `{boxes, qty}` khi chia hết, `null` khi không chia hết/đầu vào sai
- [ ] `POST /api/quality/check` + test cũ (22) không hồi quy, toàn bộ test PASS, lint/tsc/build sạch
- [ ] UI (không có hạ tầng test component): kiểm chứng bằng Chrome thật (Puppeteer, KHÔNG bấm Lưu) — scan+Enter tự Tra và nhảy ô Part#; Part# lệch → nút Lưu khoá + báo đỏ; scan đúng → mở khoá; đủ điều kiện → thùng × Pcs/Carton = TTL; `còn = 0` → PACK khoá
- [ ] Andy thử tay bằng Traveler thật (bài học phiên 009)
- [ ] CHANGELOG.md cập nhật

### 15.8 CÂU HỎI CHO ANDY (mặc định ghi sẵn — nói nếu muốn đổi)
1. **Scan 2 lần bắt buộc + chặn lưu khi Part# lệch** (15.2) — đúng ý Andy ("dùng chéo tránh lộn")? Hay chỉ cảnh báo như FID-003 v1.4 hiện tại?
2. **PACK mặc định BẬT** khi còn hàng chưa đóng (100% dòng thật có đóng thùng)?
3. **Nút gợi ý** "Đóng hết: N thùng" và "Theo lần lựa: máy" (bấm mới điền, không điền sẵn)?
4. **Reject = bảng liệt kê sẵn** (39% dòng thật có reject) + ô Special Notes luôn có. Tên lỗi ngoài danh sách (≈3% dòng: DAMAGED, MISSING TAB, DAMAGED FLANGE, CRACKED...) tạm ghi vào `Others` + Special Notes — hay thêm loại defect mới (việc mở #6)?
5. **Thứ tự/nhãn trên form** theo cột CHECKING SUMMARY (SHIFT, MC, CHECKER, TRAVELER, PART#, POT#, LOT#, No. of boxes, TTL QNT, SKID#, Reject, Notes) — tôi lấy từ file `.xlsm` trong dự án, CHƯA xem ảnh giấy "WRAPPING SUMMARY" (nằm ở `D:\AVP_AI\Data\4.WRAPPING\`, ngoài dự án). Andy đồng ý cho đọc THAM KHẢO ảnh đó để khớp nhãn, hay thứ tự trên chưa đúng?
6. **Serial tách FID riêng** (15.6) — OK? (cần trả lời 2 câu về tem/máy scan trước).

**Andy trả lời câu 1 (2026-09-19):** "thường user dùng scanner scan cả 2
barcode Traveler và Part, mục đích double check" — CHỐT: scan 2 lần là quy
trình BÌNH THƯỜNG, mục đích đối chiếu chéo → giữ 15.2 (bắt buộc scan cả 2, Part#
lệch → khoá nút Lưu). **Andy: "ok code" (2026-09-19)** → câu 2, 3, 4, 6 CHỐT theo
mặc định. **Câu 5 (đọc ảnh "WRAPPING SUMMARY" ở AVP_AI) Andy KHÔNG trả lời →
KHÔNG đọc** (CLAUDE.md nguyên tắc #5) — bố cục theo thứ tự cột CHECKING SUMMARY
trong `Data/4.WRAPPING/Wrapping_final.xlsm` (trong dự án); Andy sẽ nhận xét khi
xem giao diện thật.

### 15.9 Đồng bộ ngược cho `/factory/select` (FID-ERP-003 v1.5 — đề xuất, CHƯA làm)
FID-ERP-003 v1.4 (đã DONE) đang để ô "Part# (scan đối chiếu)" TUỲ CHỌN và chỉ
cảnh báo. Theo quy tắc chéo của Andy (15.2), nên nâng thành BẮT BUỘC + chặn lưu
khi lệch, giống Wrapping — sửa `page.tsx` + không đổi API. Chờ Andy trả lời câu 1
rồi làm CHUNG một lượt cho cả 2 trang.

## 16. THỰC HIỆN v1.5 (2026-09-19, sau khi APPROVED) — ghi chú lệch so với kế hoạch

- **Đúng kế hoạch Mục 15.4/15.7.** Route mới `GET /api/quality/lookup` (chỉ đọc, thuộc
  `/api/quality/*` — chỉ trạm Xưởng theo `lib/auth.ts`, không cần sửa phân quyền).
  Hàm thuần dùng chung với FID-ERP-003: `computeTotal`, `partMatches`, `buildDefects`,
  `sortDefectTypesByPaper` + hàm mới `suggestPackAll` ở `lib/factoryEntry.ts`.
- **Tách dùng chung 2 trang** (ngoài kế hoạch, tránh lặp code): `lib/ui/formBlocks.ts`
  (màu xám/vàng + kiểu khối/nhãn/ô) và `lib/ui/recentStorage.ts` (đọc/ghi nút gợi ý
  `localStorage`, bọc try/catch). `/factory/select` được sửa lại dùng chung 2 file này.
- **Bố cục trang**: dải đầu xám (Part#, LOT#, POT#, PO, Đã lựa/Đã đóng/Còn có thể đóng)
  → ô scan Part# (bắt buộc) → khối Wrapping (Machine, CHECKER, Shift 2 nút, nút
  "Theo lần lựa: <máy>" + "Lần trước: <người>") → Good/Hold + Special Notes (luôn hiện) →
  khối Packaging (PACK mặc định BẬT khi còn hàng chưa đóng, khoá khi còn = 0; No. of boxes,
  Pcs/Carton xám, TTL tự tính, SKID#, nút "Đóng hết: N thùng · qty") → bảng Reject liệt kê sẵn
  đủ 11 loại theo thứ tự tờ giấy → Concession (chỉ khi Hold). Thứ tự/nhãn theo cột CHECKING
  SUMMARY của `.xlsm` trong dự án — CHƯA đối chiếu ảnh giấy "WRAPPING SUMMARY" (Andy không
  trả lời câu 5 nên không đọc file ở AVP_AI).
- **Phía server không đổi** (`/api/quality/check` giữ nguyên contract + cảnh báo v1.3/v1.4).
  Ràng buộc "TTL ≤ số còn có thể đóng" UI chặn trước, server vẫn chặn như v1.1.
- **Kết quả**: `npm test` → **201/201 PASS** (7 mới: 4 lookup + 3 `suggestPackAll`; 194 cũ không
  hồi quy) · `npm run lint` sạch · `tsc --noEmit` sạch · `npm run build` thành công.
  Kiểm chứng UI bằng Chrome thật (Puppeteer, server trỏ `avp_erp_test`, KHÔNG bấm Lưu): scan
  Traveler# + Enter tự Tra và nhảy ô Part# · chưa scan Part# → Lưu khoá + nhắc bắt buộc · Part#
  lệch → báo đỏ + Lưu khoá · Part# đúng → "✓ khớp", con trỏ nhảy sang No. of boxes · nút
  "Đóng hết" (10 thùng · 1000), "Theo lần lựa: MC1" điền đúng · đủ điều kiện → Lưu mở · TTL vượt
  số còn lại → báo + Lưu khoá.
- **Chưa kiểm chứng bằng Chrome:** trạng thái PACK khoá khi `còn = 0` (chỉ suy từ code:
  `available > 0 ? (packOverride ?? true) : false`) — Andy thử bằng Traveler đã đóng hết.
- **CHƯA có xác nhận Andy đã thử trên Traveler thật.**

---
*FID-ERP-005 v1.5 — AVP_ERP | Draft 2026-09-18, v1.1–v1.4 2026-09-19, v1.5 2026-09-19*
*Tham khảo THAM KHẢO (đọc, không sửa — CLAUDE.md nguyên tắc #5):
`D:\AVP_AI\docs\features\FID-002_skid-status-good-hold.md` (nguồn UX gốc
— 2 giá trị chính + Concession đè lên Hold, KHÔNG phải giá trị thứ 3) +
`D:\AVP_AI\docs\features\FID-001_ingest-workstation-archive.md` §5.3(b)
(pattern Concession dùng chung cho qcStatus và Status skid). Dữ liệu
thật đối chiếu: `Data/4.WRAPPING/Wrapping_final.xlsm` (CHECKING SUMMARY,
cột Reject/Special Notes, kiểm chứng bằng `openpyxl` 2026-09-18).*
