# FID-ERP-002_20260918.md
# CaptureGate tương đương — cổng quét/nhập PO + Kho nguyên liệu
# Status: DONE — 21/21 test PASS.
# v1.1 (2026-09-19): Part# OCR khớp part_control theo mã gốc (Finished
# Part Number), cắt hậu tố trước khi ghi/khớp — 4 test mới + 13 cũ.
# v1.2 (2026-09-19): thêm đích "po_receive" — nút tắt quản lý cho phép,
# scan lại CHÍNH file PO để vừa đăng ký poNo vừa ghi RECEIVE luôn (bypass
# yêu cầu phiếu Traveler vật lý riêng) — 4 test mới + 17 cũ.

---

## 1. INTENT
1 cổng quét/chụp/nhập file DUY NHẤT cho Office + Máy admin (2 cổng AI đã
chốt) — chọn 1 trong 3 đích đến ("PO tham khảo", "Kho nguyên liệu", hoặc
(v1.2) "PO + Nhận nguyên liệu luôn — bypass"), Gemini OCR đọc file
(PDF/ảnh/Excel), người xem lại + xác nhận mới ghi Postgres. Xưởng KHÔNG
có quyền dùng cổng này (chốt 2026-09-17).

## 2. WHY
Tương đương `CaptureGate.tsx` đã chạy thật 9 tháng ở AVP_AI — nguyên văn
Andy (2026-09-13, `THIET_KE_HE_THONG_MOI.md`): *"gom lại 1 nút mà chức
năng chụp, scan, add đọc các loại file pdf image excel xlms và chỉ chọn
kho nào là đích đến là xong"*. Giữ đúng UX đã kiểm chứng, đổi tầng lưu
trữ (Google Sheets → Postgres qua `stock_moves`).

2 đích đến gốc không phải 2 loại tài liệu khác nhau — cùng 1 phiếu
Traveler/email PO, chỉ khác THỜI ĐIỂM và Ý ĐỊNH lưu:
- **PO tham khảo**: đọc email PO Infasco — đăng ký TRƯỚC Traveler# (dự
  báo, nguyên liệu CHƯA về thật)
- **Kho nguyên liệu**: đọc phiếu Traveler lúc nguyên liệu VỀ THẬT — xác
  nhận số lượng nhận thật

**(v1.2) "PO + Nhận nguyên liệu luôn — bypass"**: Andy nhận ra chính file
PO Infasco (`Data/1.PO/PO PO SEP 8_193853.pdf`) ĐÃ có sẵn đủ cột
Traveler#/Part#/Pot#/**Pieces** cho từng dòng — tức về mặt dữ liệu, PO
có thể dùng luôn để xác nhận RECEIVE mà không bắt buộc phải có thêm
phiếu Traveler vật lý riêng.

**Nguyên tắc SẢN PHẨM quan trọng đứng sau quyết định này** (Andy chốt
2026-09-19, sau khi làm rõ 1 phát biểu dễ hiểu lầm): thiết kế GỐC (RECEIVE
tách khỏi PO, đúng chuẩn Odoo Goods Receipt để đối chiếu "nhà cung cấp
khai" vs "AVP nhận thật") là **baseline chung, áp dụng cho MỌI khách
hàng tương lai** nếu AVP_ERP được dùng lại cho công ty khác — KHÔNG được
coi bypass này là hành vi mặc định/khuyến nghị chung. `"po_receive"` chỉ
là 1 NGOẠI LỆ CỘNG THÊM, dành riêng cho nguyên tắc vận hành RIÊNG của
AVP (tin tưởng số Infasco khai trên PO), tồn tại SONG SONG chứ KHÔNG
thay thế đích `"warehouse"` chuẩn. Đánh đổi đã xác nhận: mất khả năng
đối chiếu độc lập ở những dòng dùng bypass.

## 3. MODULE
```
Primary:  webapp/app/capture/page.tsx (UI — chọn đích đến + chọn file,
          hiện draft OCR, nút Xác nhận & Lưu)
          webapp/app/api/capture/extract/route.ts (OCR Gemini → draft,
          KHÔNG ghi DB)
          webapp/app/api/capture/confirm/route.ts (ghi Postgres sau khi
          người xác nhận — 3 nhánh theo destination, xem Mục 4)
Affected: Không có — đây là feature UI/API đầu tiên, chỉ đọc/ghi lên
          schema đã có từ FID-ERP-001
NOT:      Không làm khâu máy lựa/nhập liệu Xưởng (FID-ERP-003), không làm
          Packing Slip (FID-ERP-009), không parse Lot# thật từ email
          (FID-ERP-006 — Lot# để trống ở FID này, gán sau)
```

## 4. CONTRACT

### 4a. `POST /api/capture/extract` — chỉ đọc, KHÔNG ghi DB
Input: `multipart/form-data` — `file` (PDF/ảnh/xlsx/xlsm), `destination`
(`"po"` | `"warehouse"` | `"po_receive"` — v1.2)

Output (draft, người xem lại trước khi gọi `confirm`):
```json
{
  "ok": true,
  "destination": "warehouse",
  "rows": [
    {
      "travelerNo": "718039",
      "partNo": "11546389-T",
      "poNo": null,
      "potNo": "693",
      "qty": 45000,
      "confidence": 0.92,
      "lowConfidenceFields": []
    }
  ],
  "warnings": []
}
```
`confidence`/`lowConfidenceFields`: field nào OCR không chắc → để trống
giá trị đó (KHÔNG tự đoán, xem AI_POLICY.md §3) + liệt kê tên field vào
`lowConfidenceFields` để UI tô đỏ, bắt người điền tay.

### 4b. `POST /api/capture/confirm` — ghi Postgres, cần xác nhận CCP-1
Input:
```json
{
  "destination": "warehouse",
  "rows": [ { "travelerNo": "718039", "partNo": "11546389-T", "poNo": null, "potNo": "693", "qty": 45000 } ],
  "confirmedBy": "111"
}
```

Nhánh **`destination = "po"`**:
```sql
-- Chỉ upsert Traveler (đăng ký trước), KHÔNG ghi stock_moves —
-- nguyên liệu chưa về thật, chưa có gì để RECEIVE
INSERT INTO travelers (traveler_no, part_no, po_no) VALUES (...)
ON CONFLICT (traveler_no) DO UPDATE SET po_no = EXCLUDED.po_no
```

Nhánh **`destination = "warehouse"`** (transaction, ghi đủ hoặc không ghi gì):
```sql
-- 1. Upsert Traveler (nếu đã đăng ký từ bước "po" thì cập nhật, chưa có thì tạo mới — "vượt cấp" vẫn PASS)
INSERT INTO travelers (traveler_no, part_no, pot_no) VALUES (...)
ON CONFLICT (traveler_no) DO UPDATE SET pot_no = EXCLUDED.pot_no
-- 2. Ghi RECEIVE vào stock_moves (RECEIVE không bắt buộc machine/operator/shift, xem FID-ERP-001)
INSERT INTO stock_moves (traveler_no, move_type, qty, source_station, device_id, created_at)
VALUES (..., 'RECEIVE', ..., 'OFFICE'|'ADMIN', ..., now())
```

Nhánh **`destination = "po_receive"` (MỚI v1.2)** — dùng CHUNG code path
với `"warehouse"` (cùng validate `qty>0`, cùng ghi `RECEIVE` trong 1
transaction), CHỈ khác upsert Traveler có thêm `po_no` (vì OCR đọc được
`poNo` từ chính file PO):
```sql
INSERT INTO travelers (traveler_no, part_no, po_no, pot_no) VALUES (...)
ON CONFLICT (traveler_no) DO UPDATE SET po_no = EXCLUDED.po_no, pot_no = EXCLUDED.pot_no
INSERT INTO stock_moves (traveler_no, move_type, qty, source_station, device_id, created_at)
VALUES (..., 'RECEIVE', ..., 'OFFICE'|'ADMIN', ..., now())
```
`poNo` CHỈ được set khi thật sự có giá trị non-empty (không đưa
`po_no = NULL` vào update) — tránh xoá mất `poNo` Traveler đã có từ
trước nếu dòng gửi lên không kèm `poNo`.

Output:
```json
{ "ok": true, "saved": 1, "skippedDuplicates": [], "moveIds": [123] }
```

## 5. RULES
MUST:
  - **Draft trước, ghi sau** — `extract` KHÔNG BAO GIỜ ghi DB, chỉ
    `confirm` mới ghi, và `confirm` chỉ chạy sau khi người bấm nút (không
    có đường tự động nào gọi thẳng `confirm` mà bỏ qua `extract` + màn
    hình xem lại) — đúng CCP-1/AI_POLICY.md §1.
  - **Trùng lặp phải cảnh báo, không tự động bỏ qua hay tự động ghi đè**
    — trước khi `confirm`, kiểm tra `travelerNo` đã có `stock_moves` loại
    `RECEIVE` chưa (destination `warehouse`): nếu CÓ RỒI → cảnh báo rõ
    "Traveler X đã nhận hàng trước đó", người tự quyết có ghi thêm 1 dòng
    RECEIVE nữa hay huỷ (giống hành vi đã kiểm chứng ở AVP_AI
    `TravelerSection.tsx` — không âm thầm bỏ qua, không âm thầm ghi đè).
  - **`destination = "po"` KHÔNG tạo `stock_moves`** — chỉ đăng ký
    `travelers.poNo` trước. Một Traveler chỉ CÓ THẬT (coi là đã nhận
    hàng) khi có ít nhất 1 dòng `stock_moves` loại `RECEIVE` — đây là
    cách phân biệt "dự báo" vs "đã nhận thật" mà KHÔNG cần thêm cột
    status riêng trên `travelers` (tận dụng đúng nguyên tắc `stock_moves`
    là nguồn sự thật duy nhất, xem FID-ERP-001 §5).
  - **`sourceStation`/`deviceId`** trên mỗi `stock_moves` ghi đúng theo
    người đang dùng (OFFICE hoặc ADMIN) — cơ chế xác định danh tính thật
    (session/auth) THUỘC FID-ERP-011, FID này tạm nhận qua tham số request
    (KHÔNG cho client tự khai báo `sourceStation` tuỳ ý mà server tin
    luôn — xem GPT phản biện vòng 2, FINAL_DECISION.md — phải validate
    tối thiểu giá trị nằm trong `('OFFICE','ADMIN')`, chặn `FACTORY` ở
    route này vì Xưởng không được dùng cổng này).
  - **OCR không chắc → để trống, không tự đoán** — field nào
    `confidence` thấp thì `extract` trả về `null`/rỗng cho field đó, liệt
    kê trong `lowConfidenceFields`, UI bắt buộc người điền tay trước khi
    `confirm` (không cho Xác nhận nếu field bắt buộc còn trống — xem Test
    Criteria).
  - `qty` gửi lên `confirm` phải > 0 (khớp CHECK constraint FID-ERP-001,
    nhưng validate sớm ở API để trả lỗi rõ ràng thay vì lỗi SQL thô).
  - **(v1.1) `partNo` OCR đọc từ "Part#" PHẢI cắt hậu tố trước khi
    upsert/khớp `part_control`** — chứng từ thật có 2 field khác nhau:
    "Part#" (có hậu tố, vd `11546389-T`) và "Finished Part Number" (mã
    gốc, vd `11546389`) — Andy xác nhận đây là CÙNG 1 sản phẩm cho mục
    đích đóng gói/qty-per-box (2026-09-19, xem
    `Data/2. Traveler/TRAVELER SHEETS SEP 9.pdf`). Cắt bằng danh sách
    hậu tố biết trước (`lib/part.ts` → `stripPartSuffix`, nguồn
    `Data/BANG_MA_THAM_CHIEU_AVP_2026-09-17.xlsx` sheet `2_Hau_To_Part`)
    — KHÔNG cắt bừa theo dấu "-" đầu tiên vì nhiều Part# thật có "-" là
    một phần mã gốc (vd `1015463X-03`, `100-5829`, kiểm chứng trong sheet
    `1_Part_Control`).
  - **(v1.2) `destination = "po_receive"` dùng CHUNG quyền + validate với
    `"warehouse"`** — chỉ OFFICE/ADMIN, chặn FACTORY, `qty` phải > 0 (trừ
    dòng GAYLORD). Cả OFFICE lẫn ADMIN đều dùng được (Andy xác nhận
    2026-09-19) — "quản lý cho phép" ở đây là quyết định BẬT/dùng tính
    năng này cho 1 PO cụ thể, không phải giới hạn theo trạm/vai trò.
MUST NOT:
  - Không tạo Lot# — trường `lotNo` để trống ở FID này (thuộc FID-ERP-006).
  - Không cho phép `destination` khác `"po"`/`"warehouse"`/`"po_receive"`
    (Finish Good scan là phạm vi khác, chưa thiết kế ở FID này).
  - Không cho `sourceStation = "FACTORY"` đi qua route này (Xưởng không
    có cổng AI, chặn ở tầng API chứ không chỉ ẩn ở UI).

## 6. EXAMPLE
Traveler `718039`, Part# `11546389-T`, Pot# `693` (phiếu mẫu thật `Data/2.
Traveler/TRAVELER SHEETS SEP 9.pdf`):
```
Bước 1 (destination="po", lúc có email PO, CHƯA có hàng):
  extract → draft { travelerNo: "718039", partNo: "11546389-T", poNo: "PO-2026-xxx" }
  confirm → travelers upsert (po_no="PO-2026-xxx"), KHÔNG có stock_moves nào

Bước 2 (destination="warehouse", vài ngày sau, hàng về thật):
  extract → draft { travelerNo: "718039", partNo: "11546389-T", potNo: "693", qty: 45000 }
  confirm → travelers upsert (pot_no="693") + stock_moves 1 dòng RECEIVE qty=45000
```
Edge: Nếu bỏ qua bước 1 (không có email PO trước, hàng về luôn) → bước 2
vẫn PASS bình thường (`ON CONFLICT DO UPDATE` tự tạo Traveler mới nếu
chưa có) — không bắt buộc phải qua đích "po" trước.

Edge: Nhận hàng traveler `718039` LẦN 2 (hàng về đợt 2 cùng Traveler#, có
thật ở dữ liệu mẫu — số lượng lớn chia nhiều đợt) → cảnh báo trùng, người
xác nhận vẫn ghi thêm 1 dòng RECEIVE mới (không phải lỗi, là nghiệp vụ
thật), sổ cái cộng dồn đúng theo append-only.

**(v1.2) Nút tắt "po_receive"** — PO thật `Data/1.PO/PO PO SEP 8_193853.pdf`
(traveler `716958`, Part# `11549168-CA-IN-B`, Pot# `1837`, Pieces `51,000`):
```
destination="po_receive" (thay vì phải scan PO trước rồi scan thêm 1
phiếu Traveler vật lý riêng — bỏ qua bước 2):
  extract → draft { travelerNo: "716958", partNo: "11549168-CA-IN-B",
                     poNo: "193853", potNo: "1837", qty: 51000 }
  confirm → travelers upsert (po_no="193853", pot_no="1837",
             part_no="11549168" sau khi cắt hậu tố v1.1)
            + stock_moves 1 dòng RECEIVE qty=51000, TRONG 1 LẦN quét
```
Đánh đổi: mất mốc đối chiếu độc lập "Infasco khai trên PO" vs "AVP xác
nhận lại qua phiếu Traveler vật lý" — số RECEIVE giờ = ĐÚNG số PO OCR
đọc được, không có bước xác nhận chéo thứ 2.

## 7. TEST CRITERIA
- [ ] `extract` với destination="po" → không có lệnh ghi DB nào chạy (mock/spy transaction, xác nhận 0 write)
- [ ] `confirm` destination="po" → chỉ có `travelers` thay đổi, `stock_moves` không có dòng mới
- [ ] `confirm` destination="warehouse" → có đúng 1 dòng `stock_moves` loại RECEIVE + `travelers` upsert, trong 1 transaction (test rollback nếu 1 trong 2 fail)
- [ ] `confirm` với `sourceStation="FACTORY"` → bị chặn ở API (400), không tới được DB
- [ ] `confirm` với `qty <= 0` → bị chặn ở API trước khi chạm CHECK constraint
- [ ] Traveler đã có RECEIVE trước đó, gọi `confirm` warehouse lần 2 → có cảnh báo trong response (`skippedDuplicates` hoặc field cảnh báo tương đương), nhưng VẪN cho ghi nếu người xác nhận muốn (không tự động chặn cứng)
- [ ] `extract` trả `lowConfidenceFields` khi 1 field OCR đọc không chắc — field đó `null` trong `rows`, không có giá trị bịa
- [ ] UI: nút "Xác nhận & Lưu" disable khi còn field bắt buộc trống (travelerNo/partNo/qty ở destination warehouse)
- [ ] (v1.2) `confirm` destination="po_receive" → `travelers` có cả `poNo` VÀ `potNo`, đúng 1 dòng `stock_moves` RECEIVE, trong 1 transaction
- [ ] (v1.2) `confirm` destination="po_receive" với `sourceStation="FACTORY"` → 400, không tới DB
- [ ] (v1.2) `confirm` destination="po_receive" với `qty<=0` → 400
- [ ] (v1.2) Gửi dòng KHÔNG kèm `poNo` (destination warehouse/po_receive) → KHÔNG xoá `poNo` Traveler đã có sẵn từ trước
- [ ] Tất cả test FID-ERP-001 vẫn PASS (no regression)
- [ ] CHANGELOG.md cập nhật

## 8. NOT IN SCOPE
- Khâu máy lựa/nhập liệu tại Xưởng (FID-ERP-003 — khác route, Xưởng
  không dùng CaptureGate)
- Parse Lot# thật từ nội dung email (FID-ERP-006)
- Destination "Finish Good" (đóng gói/scan) — thuộc phạm vi FID-ERP-009
  (Packing Slip), chưa thiết kế ở FID này
- Cơ chế xác thực danh tính thật cho `sourceStation`/`deviceId`
  (session/login) — thuộc FID-ERP-011, FID này chỉ validate giá trị nằm
  trong tập cho phép
- Voice input (🎤) — đã thấy trong CaptureGate.tsx AVP_AI nhưng ghi rõ
  "sắp có — giai đoạn 2", chưa làm ở AVP_AI thật nên cũng chưa làm ở đây

## 9. FILES
New:    webapp/app/capture/page.tsx
        webapp/app/api/capture/extract/route.ts
        webapp/app/api/capture/confirm/route.ts
        webapp/lib/ocr/gemini.ts (gọi Gemini Vision — model cụ thể [TO BE CONFIRMED, xem PROJECT_INFO_FORM.md §1])
        webapp/tests/integration/capture.test.ts
        webapp/lib/part.ts (v1.1 — `stripPartSuffix`)
Update: CHANGELOG.md (bắt buộc)
        webapp/lib/ocr/gemini.ts (v1.2 — thêm prompt cho `po_receive`)
        webapp/app/capture/page.tsx (v1.2 — thêm nút đích thứ 3)

## 10. THỰC HIỆN

**v1.1 (2026-09-19)** — Andy dán `GEMINI_API_KEY` thật (copy từ
`D:\AVP_AI\webapp\.env.local`, xem Mục 4 việc đang mở phiên trước) rồi tự
thử `/capture` destination="po" với 1 file PO thật (7 dòng) qua trình
duyệt — gặp lỗi `Foreign key constraint ... travelers_part_no_fkey`
(500) vì 4 Part# OCR đọc được (`11549168-CA-IN-B`, `06504080-T`,
`11561645-HT`, `1K2GZ0-00-A`) chưa có trong `part_control`.

Khi kiểm tra, phát hiện gốc rễ sâu hơn "thiếu dữ liệu": Andy chỉ ra Part#
có hậu tố (đọc từ field "Part#") và mã gốc không hậu tố (field "Finished
Part Number" trên cùng phiếu) là CÙNG 1 sản phẩm — xác nhận bằng phiếu
thật `TRAVELER SHEETS SEP 9.pdf` (Part# `11546389-T` = Finished Part
Number `11546389`). Đây từng là câu hỏi mở CHƯA trả lời từ 2026-09-13
(`BANG_MA_THAM_CHIEU_AVP_2026-09-17.xlsx` sheet `2_Hau_To_Part`, "ý
nghĩa từng hậu tố CHƯA xác nhận") — Andy chốt tại đây: cắt hậu tố, khớp
theo mã gốc, KHÔNG giữ hậu tố khi ghi `travelers.partNo`.

Viết `lib/part.ts` (`stripPartSuffix`) — bóc lặp hậu tố theo danh sách
biết trước (ưu tiên hậu tố 2 đoạn trước 1 đoạn, vd cắt hết
`-CA-IN-B` thay vì chỉ `-B`), KHÔNG cắt theo dấu "-" đầu tiên vì kiểm
chứng trong `1_Part_Control` có nhiều Part# thật dùng "-" làm một phần
mã gốc (`1015463X-03`, `100-5829`, `07336-54-D01`...) — cắt bừa sẽ gộp
nhầm 2 sản phẩm khác nhau thành 1. Áp dụng ở cả 2 nhánh `confirm`
(po/warehouse). Thêm 3 dòng `part_control` xác nhận thật vào DB dev
(`11549168` qty=1000, `11561645` qty=4000, `06504080` qty=6000, đều
Infasco — tra chéo `FINISHED PALLET REPORT_final.xlsm` sheet
`PartControl` nội bộ + file tổng hợp). Riêng `1K2GZ0-00-A` (kể cả sau
khi cắt hậu tố `1K2GZ0-00`) **vẫn không có ở bất kỳ nguồn nào** — chưa
thêm vào DB, để dành hỏi Andy (xem `docs/records/LATEST_SESSION.md`
Mục 4 việc đang mở).

**Cập nhật cùng ngày (2026-09-19, sau khi Andy yêu cầu)**: đã tổng hợp
TOÀN BỘ `part_control` từ 4 nguồn thật — `BANG_MA_THAM_CHIEU_AVP_2026-09-17.xlsx`
(`1_Part_Control`), `FINISHED PALLET REPORT_final.xlsm` (`PartControl` +
`WorkStationArchive` — sản lượng thật, dùng `# of boxes × Unit Quantity =
Total Quantity` làm bằng chứng dự phòng khi không có PartControl chính
thức), `Wrapping_final.xlsm` (`QuantityControl`, đối chiếu thêm). Chuẩn
hoá TẤT CẢ theo mã gốc (`stripPartSuffix`) trước khi gộp — 1319 dòng thô
→ **1235 mã gốc duy nhất**. Kết quả: **1045 mã đầy đủ + không xung đột**
đã nạp thẳng vào `part_control` (dev), riêng `1K2GZ0-00` nạp tay thêm
(xác nhận qua PO thật `Data/1.PO/PO PO SEP 8_193853.pdf`, letterhead
"Infasco Nut LP") = **1046 dòng** trong DB. Còn lại 190 mã (160 thiếu
Quantity/box, 20 thiếu Client, 11 có xung đột số liệu giữa các nguồn) —
KHÔNG nạp, để nguyên trong file
`Data/PART_CONTROL_MASTER_2026-09-19.xlsx` (sheet `CAN_XEM_LAI`) kèm cột
ghi chú rõ cần Andy bổ sung gì, cập nhật sau. 164/164 test vẫn PASS (no
regression — việc nạp dữ liệu không đổi code/schema/test).

**v1.2 (2026-09-19, cùng phiên)** — Trong lúc bàn về ý nghĩa "Kho nguyên
liệu" (RECEIVE tách khỏi PO đúng chuẩn Odoo Goods Receipt, để đối chiếu
"Infasco khai" vs "AVP nhận thật"), Andy yêu cầu thêm 1 nút tắt: khi
"quản lý cho phép", scan LẠI CHÍNH file PO để chấp nhận số Pieces trên
đó làm luôn số RECEIVE — bỏ qua yêu cầu có phiếu Traveler vật lý riêng.
Xác nhận 2 điểm trước khi code: (1) lấy số bằng cách OCR lại chính PO
(không phải copy số đã lưu từ bước "po" trước đó — tránh phải đổi
schema Traveler để tạm lưu "qty dự báo"); (2) cả OFFICE lẫn ADMIN dùng
được, KHÔNG đánh dấu riêng dòng RECEIVE nào là bypass trong sổ cái
(chấp nhận đánh đổi mất khả năng phân biệt sau này).

Thêm đích `"po_receive"` (`lib/ocr/gemini.ts` — prompt đọc ĐỦ 5 field
travelerNo/partNo/poNo/potNo/qty từ 1 file PO, khác `"po"` chỉ đọc 3
field không có qty). Route `confirm` dùng CHUNG code path với
`"warehouse"` (qua tập `RECEIVE_DESTINATIONS`) — chỉ khác upsert
Traveler có thêm `poNo` khi giá trị non-empty (dùng spread
`...poNoField` thay vì gán thẳng, để KHÔNG vô tình ghi đè `poNo` = NULL
lên Traveler đã có `poNo` từ trước khi dòng gửi lên không kèm field
này — áp dụng luôn cho cả nhánh `"warehouse"` cũ, phòng trường hợp
client gửi `poNo: null` tường minh). UI thêm nút thứ 3 "3. PO + Nhận
nguyên liệu luôn (bypass)", bảng hiện đủ cột PO#/Pot#/Qty. 4 test mới
(ghi cả `poNo`+`potNo`+RECEIVE trong 1 transaction, chặn FACTORY, chặn
`qty<=0`, không xoá `poNo` cũ khi dòng mới không gửi kèm) + 2 test UI
`isCaptureRowIncomplete` + 17 test cũ = **21/21 PASS**, lint sạch, build
thành công (`next build` qua hết 26 route).

---
*FID-ERP-002 v1.0 — AVP_ERP | Draft 2026-09-18*
*Tham khảo THAM KHẢO (đọc, không sửa — CLAUDE.md nguyên tắc #5):
`D:\AVP_AI\webapp\src\components\CaptureGate.tsx` + `TravelerSection.tsx`
+ `D:\AVP_AI\docs\features\FID-001_ingest-workstation-archive.md` +
`D:\AVP_AI\THIET_KE_HE_THONG_MOI.md` §17.1. Viết lại CONTRACT theo
Postgres/stock_moves thật, không copy nguyên field tên cột Sheet.*
